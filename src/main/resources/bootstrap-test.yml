micronaut:
  application:
    name: analytics
  security:
    enabled: false
  config-client:
    enabled: false
consul:
  client:
    registration:
      enabled: false
    config:
      enabled: false